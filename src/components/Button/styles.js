import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: {
    padding: 14,
    borderRadius: 8,
    alignItems: 'center',
    marginVertical: 8
  },
  text: {
    fontSize: 16,
    fontWeight: '600',
    color: 'white'
  }
});

export default styles;